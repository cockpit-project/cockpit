(function(require) {
    require.config({
        baseUrl: "../",
        waitSeconds: 30,
        paths: {
            "jquery": "base1/jquery",
            "react": "base1/react",
        }
    });
})(require);

