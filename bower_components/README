# Cockpit Javascript Runtime Dependencies

Cockpit uses various javascript dependencies at runtime in the browser
such as jQuery. These are retrieved via Bower and live in the
bower_components/ directory.
