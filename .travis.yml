notifications:
  irc:
    channels:
     - "chat.freenode.net#cockpit"
    skip_join: true
    use_notice: true
    template:
     - "%{message} (%{branch} - %{commit} : %{author}): %{build_url}"
language: c
compiler:
  - gcc
  - clang
cache: apt
script:
  - gcc ./tools/flood.c -o flood; ./flood; echo code $?
