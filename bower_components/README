# Cockpit Javascript Build time dependencies

Cockpit uses various javascript dependencies at runtime in the browser
such as jQuery, React, or term.js. These dependencies are built into Cockpit
during the build process.

These build time dependencies are retrieved via Bower and live in the
of the bower_components/ directory.
